import { initToggleVisibilityPassword } from "../../utils/toggleVisibilityPassword.js";
import { initLoadingForm } from "../../utils/loadingForm.js";

$(async function () {
    initToggleVisibilityPassword();

    initLoadingForm();
});
